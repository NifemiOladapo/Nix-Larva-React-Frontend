import Header from "./Header";

const HomePage = () => {
  return (
    <div
      style={
        {
          // height: "100vh",
          // display: "flex",
          // justifyContent: "center",
          // alignItems: "center",
        }
      }
    >
      <Header />
    </div>
  );
};

export default HomePage;
